import { Component } from '@angular/core';

@Component({
    selector: 'nav',
    template: `
                <mat-toolbar color="primary">
                    Temperature Display
                </mat-toolbar>
              `
})

export class NavComponent {

    constructor() {}
}